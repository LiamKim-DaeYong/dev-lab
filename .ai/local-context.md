# dev-lab Local Context

이 repo는 학습 개념을 코드로 검증하는 실험실입니다.

## 핵심 원칙

- 모든 실험은 가설 → 실험 → 측정 → 결론 구조
- before/after 지표를 가능한 한 항상 기록
- 실험 결과가 블로그 글과 포트폴리오의 근거가 됨
- Kotlin 기반, 필요 시 Java 혼용 가능
- 마크다운 fenced code block은 항상 언어 태그 명시 (`text`, `bash`, `powershell`, `yaml`, `json` 등)
