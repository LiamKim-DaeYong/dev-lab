services:
  db:
    image: postgres:16-alpine
    container_name: db-foundation-plan01
    environment:
      POSTGRES_DB: labdb
      POSTGRES_USER: lab
      POSTGRES_PASSWORD: lab
    ports:
      - "55436:5432"
    volumes:
      - ./sql:/scripts
