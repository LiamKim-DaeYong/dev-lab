services:
  db:
    image: postgres:16-alpine
    container_name: db-foundation-plan03
    environment:
      POSTGRES_DB: labdb
      POSTGRES_USER: lab
      POSTGRES_PASSWORD: lab
    ports:
      - "55438:5432"
    volumes:
      - ./sql:/scripts
