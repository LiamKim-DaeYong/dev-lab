services:
  db:
    image: postgres:16-alpine
    container_name: db-foundation-plan02
    environment:
      POSTGRES_DB: labdb
      POSTGRES_USER: lab
      POSTGRES_PASSWORD: lab
    ports:
      - "55437:5432"
    volumes:
      - ./sql:/scripts
